import React from 'react';

const Dialogs = (props) => {
    // if (props.users === null) {
    //     return <div>Loading...</div>
    // }
    return (<div>

        </div>
    );
};

export default Dialogs;